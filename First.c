#include<stdio.h>
int main() 
{
    int number = 23;
    char stri = 's';

    // string name = "sonu"; string is not defined
    // boolean name = true; Boolean is not defined
    printf("hello World\n"); //back slash n (\n) is defined next line ya break line
    printf("hello World\n"); //back slash n (\n) is defined next line ya break line
    printf("variable number is %d" , number);
    // printf("pi value is %d" , pi);
    printf("charecter is %c", stri);
    return 0;
}
